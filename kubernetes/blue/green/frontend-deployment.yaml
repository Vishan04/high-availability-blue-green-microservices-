apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
      version: green
  template:
    metadata:
      labels:
        app: frontend
        version: green
    spec:
      containers:
        - name: frontend
          image: blue-green-frontend:v2
          imagePullPolicy: Never
          ports:
            - containerPort: 5000

---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: green
spec:
  selector:
    app: frontend
    version: green
  ports:
    - port: 5000
      targetPort: 5000